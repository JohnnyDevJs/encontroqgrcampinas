/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: { domains: ['static.wixstatic.com', 'comunidadevinhedocampinas.com.br'] }
}

module.exports = nextConfig